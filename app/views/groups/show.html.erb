<h2><%= image_tag @group_name.icon ? @group_name.icon : 'expense.png' %>
  <%= @group_name.name %>
</h2>
<% @group_expenses.each do |expense| %>
<%= render 'expenses/expense-card', expense: expense %>
<% end %>