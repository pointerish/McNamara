<%= image_tag expense.icon %>
<%= link_to expense.name, expense_path(expense.id) %>
<small>$<%= expense.amount %> | Created on: <%= expense.created_at.strftime("%m/%d/%Y") %></small>
<%= link_to "Delete", expense_path(expense.id), method: :delete %>
<%= link_to "Edit", edit_expense_path(expense.id) %>